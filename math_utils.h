#ifndef MATH_UTILS_H
#define MATH_UTILS_H

// Cách 1: Không cần viết extern, vì mặc định đã là extern
extern int sum(int a, int b);

// Cách 2: Viết rõ extern (cũng được)
extern int multiply(int a, int b);

#endif
